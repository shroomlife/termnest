<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="TermNest.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TermNest.App"
    xmlns:shell="using:TermNest.App.Shell"
    xmlns:tb="using:H.NotifyIcon"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="TermNest (alpha)"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="TermNest (alpha)" Subtitle="WinUI 3 preview">
            <TitleBar.IconSource>
                <BitmapIconSource UriSource="ms-appx:///Assets/Square44x44Logo.targetsize-24.png"
                                  ShowAsMonochrome="False" />
            </TitleBar.IconSource>
        </TitleBar>

        <shell:ShellLayout x:Name="Shell" Grid.Row="1" />

        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="/Assets/AppIcon.ico"
                        ToolTipText="TermNest"
                        LeftClickCommand="{x:Bind ShowFromTrayCommand, Mode=OneTime}">
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Show TermNest" Click="OnTrayShowClick">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE8A7;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Text="Exit" Click="OnTrayExitClick">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE7E8;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
    </Grid>
</Window>
