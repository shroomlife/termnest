<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="TermNest.App.Controls.EmbeddedPuttyHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Layout-only placeholder. The owned PuTTY top-level window is
         positioned over this Border's screen rect. Background is transparent
         so the WinUI compositor never paints over the embedded HWND. -->
    <Border x:Name="HostPlaceholder" Background="Transparent" />
</UserControl>
